export 'runtime_data_profile.dart';

/// Runtime database paths are profile-scoped in [RynRuntimeDataPathContract].
///
/// This file remains as a compatibility import boundary. It intentionally
/// exposes no untyped default path that could blur development and production.
